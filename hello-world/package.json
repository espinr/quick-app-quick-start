{
  "name": "hello-world",
  "version": "1.0.0",
  "description": "Hello World sample for Quick Apps",
  "engines": {
    "node": ">=8.10"
  },
  "scripts": {
    "start": "hap server --watch",
    "build": "hap build",
    "release": "hap release",
    "server": "hap server",
    "watch": "hap watch",
    "lint": "eslint --format codeframe --fix --ext .ux,.js src/"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^6.8.0",
    "eslint-plugin-ux": "^0.0.4",
    "hap-toolkit": "^1.9.5"
  },
  "browserslist": [
    "chrome 65"
  ]
}